using UnityEngine;

public class PlayerStatManager : StatManager,IGetCompoable
{
    public override void AddStatMod(StatModifierSO statMod)
    {
        //_parent.GetCompo<AgentManager>(true).Units
        //base.AddStatMod(statMod);
    }
}
